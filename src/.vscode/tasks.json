{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PreLaunchTask",
            "type": "shell",            
            "command": "bash ./preLaunchTask.sh --webapi",
            "options": {
                "cwd": "${workspaceFolder}/.."
            }
        },
        {
            "label": "PostDebugTask",
            "type": "shell",            
            "command": "bash ./postDebugTask.sh --webapi",
            "options": {
                "cwd": "${workspaceFolder}/.."
            }
        }
    ]
}